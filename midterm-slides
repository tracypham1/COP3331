Pointers, Structs, Enums
- Dynamic arrays allocate memory and free the memory
    - EX: array_type* arr = new array_type[size];
          delete[] arr;
- Strings
    - EX: string str; //initialize an empty string
    - initialize the string with either
        - EX: string str("What's poppin world!");
        - EX: const char* str("Hello"); 
        //must have the const or else error C++ does not allow convert from string to char*
    - to get how many character in string 
        - EX: str.size() OR str.length()
//nvm gotta write this ish so I can bring it to the exam, duh.